<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.comp3350_group10.bookstore">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookStore">
        <activity android:name=".presentation.DeleteUserActivity"></activity>
        <activity android:name=".presentation.ContactUsActivity" />
        <activity android:name=".presentation.CreateUserActivity" />
        <activity
            android:name=".presentation.UserSettingActivity"
            android:label="@string/title_activity_user_setting" />
        <activity
            android:name=".presentation.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.BookStore.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.BookDetailsActivity"
            android:label="@string/title_activity_book_details"
            android:theme="@style/Theme.BookStore.NoActionBar" />
        <activity
            android:name=".presentation.LoginActivity"
            android:label="@string/title_activity_login" />
    </application>

</manifest>